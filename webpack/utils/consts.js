const PORT = 80
const SRC = 'src'
const DIST = 'dist'
const ENTRY = 'src/entry.js'
const TEMPLATE = 'src/index.html'
const TITLE = 'react webapp'
// 这个是生产环境下的目录
const CDN = '/Dvt-Mobile/'

module.exports = {
  PORT,
  SRC,
  DIST,
  ENTRY,
  TEMPLATE,
  TITLE,
  CDN
}
